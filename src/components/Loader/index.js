import React from "react";
import loader from "../../assets/loader.gif";

const Loader = props => (
  <div>
    <img style={{ width: 75 }} alt="loader-icon" src={loader} />
  </div>
);

export default Loader;
